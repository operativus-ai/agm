Role: Act as a Senior Frontend Architect.

Objective: > 1. Analyze the frontend project (in /frontend directory)in the workspace to extract its architectural "DNA." 2. Generate a comprehensive Implementation Plan for a brand-new, performance-optimized React project called agentmanager-ui from scratch.

Step 1: Extraction & Analysis

Use the list_directory and read_file tools to map the current file structure.

Identify core UI frameworks (e.g., Tailwind, DaisyUI, Shadcn/ui, Material UI) and state management patterns (e.g., Zustand, Redux, Context API).

Extract the dependencies and scripts from package.json.

Step 2: Scaffolding Plan (Output as Artifact)

Create an Implementation Plan for a new React project (Vite-based).

Define a modern file structure (e.g., Feature-based or Atomic design).

Include a setup_commands list for the terminal to initialize the project, install identical dependencies, and configure the UI framework.

Step 3: Component Blueprint

Identify the 3 most critical UI components in the current app.

Provide a "Clean Code" version of these components for the new project using TypeScript and functional patterns.

Constraint: Do not execute any npm install or mkdir commands yet. Present the Task List and Implementation Plan for my review first.