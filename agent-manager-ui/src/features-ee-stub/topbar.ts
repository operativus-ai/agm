import type { ReactNode } from 'react';

/**
 * Core stub for the `@ee/topbar` seam: widgets rendered in the dashboard's top
 * header, left of the system indicators. The enterprise web build aliases this
 * to its real manifest (e.g. the super-admin org switcher); Core renders none.
 */
export const eeTopbarWidgets: ReactNode[] = [];
